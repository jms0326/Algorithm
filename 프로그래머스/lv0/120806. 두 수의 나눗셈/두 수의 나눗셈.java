class Solution {
    public int solution(int num1, int num2) {
        int answer = 0;
        
        return (num1 * 1000) / num2;
        
        
        
        //double re = num1 / num2;
        
        // answer = re.intValue();
        //answer = (int) Math.round(re);
        //return answer * 1000;
    }
}